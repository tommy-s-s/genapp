<%= form_with(model: yakushoku) do |form| %>
  <% if yakushoku.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(yakushoku.errors.count, "error") %> prohibited this yakushoku from being saved:</h2>

      <ul>
        <% yakushoku.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :yaku_name, style: "display: block" %>
    <%= form.text_field :yaku_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
