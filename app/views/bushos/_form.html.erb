<%= form_with(model: busho) do |form| %>
  <% if busho.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(busho.errors.count, "error") %> prohibited this busho from being saved:</h2>

      <ul>
        <% busho.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :busho_name, style: "display: block" %>
    <%= form.text_field :busho_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
